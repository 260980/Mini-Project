#include<stdio.h>
#include "header.h"
#include<math.h>

/**
 * @brief returns square root of a number
 * 
 * @param n1 
 * @param res 
 * @return verify 
 */

verify calc_square(dt n1,result* res)
{
    res->final_ans=sqrt(n1.n1);
    return SUCCESS;
}
